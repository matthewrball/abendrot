I'm resuming the **Abendrot** project — a free, open-source, native macOS menu-bar app that warms screen color temperature across every display (built-in + external) for circadian health. This directory (`Documents/abendrot`) is the canonical home.

Before doing anything, read these in full:
1. `HANDOFF.md` — full context, locked decisions, gotchas, and the "Session continuity" notes.
2. `docs/abendrot-plan.md` — the master plan. Read it all, but especially: §15 (execution lanes), §16 (confirmed decisions), §6 + §21.1 (engine architecture + audit safety refinements), §5 + §21.3/21.4 (brand + Liquid Glass), §9 + §21.2 (release/DMG), §8 (QA).
3. Skim `brand/brand-direction.md` and `docs/research/` (research sweeps, `plan-audit-ccg`, `reference-macos-app-skills`, `name-clearance`).

**Status:** the plan is **APPROVED for execution**. Everything is locked — name **Abendrot**, domain **abendrot.app** (purchased), **MIT**, **native Swift/SwiftUI on macOS 26 "Tahoe", Xcode 26**, circadian-health-first positioning, Developer-ID + notarized (no Mac App Store), **Aptabase** opt-in analytics, **staged-beta release strategy** (0.1 overlay → 0.2 DDC opt-in → 0.3 Sparkle → 1.0 branded launch after the hardware matrix passes), and the **provisional** brand direction **Ember amber `#FFAB5C` + "Sunset arc" icon** (still to be refined en masse).

**Now kick off execution** via OMC **`/team`** across **all six lanes (A–F)** in plan §15, dispatching heavy backend to **Opus 4.8 `/goal`** sessions (max effort) and keeping the hardest engine logic + design-taste/selection in the lead session. Use **ultracode / workflows** for the parallel work. **Confirm the lane + sequencing plan with me first, then go.**

Sequencing is driven by dependencies, not letters — here's what starts now vs. what's gated:

**Wave 1 — start immediately, in parallel:**
- **Lane A — Warmth engine (`WarmthKit`; the long pole):** write the spec from §6 + §21.1 — module split `WarmthCore` / `DisplayServices` / `HardwareDDC` / `OverlayRenderer` / `NightShiftBridge` / `AbendrotApp`; **overlay is the default layer; DDC opt-in per display with restore tooling + an emergency "Restore Displays" command; a `DisplayIdentity` (EDID) model; a private-API kill switch + overlay-only fallback; and NO default screen-capture gamma probe** (capability-classify instead — see §21‑E1). Freeze the engine API contract early so B/D can build against it. Then dispatch to an Opus 4.8 `/goal`. Target **0.1 = overlay + hold-to-reveal hotkey + 3 schedule modes + branded DMG + notarization** first.
- **Lane C — Brand & design system (gates B + D — do it early):** run the iterate-en-masse exercise (§5.5 / §21.4) — 3-3-1 icon variations on the Sunset-arc direction, the 18px "vibrant" menu-bar template (glows amber when active, survives wallpaper desktop-tinting), finalized Ember-amber tokens + dark/light, then the Liquid Glass component mockups. **Keep me in the loop for icon + screen selection.** Starting artifact: `brand/explorations/index.html` (re-serve: `python3 -m http.server 8733 --directory brand/explorations`).
- **Lane E — Release / CI:** stand up an **M0 signed + hardened + notarized smoke build** and the **two-mode DMG** (pretty Finder window + deterministic `hdiutil` fallback) early per §21.2; choose ONE Sparkle release authority; draft the GitHub Actions split (hosted lint/unit/archive + self-hosted display matrix); fork the Go release CLI. Gate every release on ≥1 notarized + stapled DMG that CI mounts and verifies.
- **Lane F — Content & GTM:** draft the conversion-focused README, privacy policy, cited non-medical science snippets (§13), and the Product Hunt / Show HN / Reddit copy + launch timeline (§14, using the §21.5 channel-tailored lead). Fully parallelizable.

**Wave 2 — start once the Wave-1 contracts are stable:**
- **Lane B — App UI / UX** (depends on brand tokens from C + engine API from A): `MenuBarExtra`, simple popover, advanced "liquid expansion", Settings (programmatic Liquid Glass window), onboarding ("3 clicks to warmth"), hide-from-menu-bar, reveal-true-color wiring, plus the Tahoe-native glass polish — specular tracking, material hierarchy, ember-tinted Reduce-Transparency fallback (§21.3). Route motion through `/design-motion-principles`.
- **Lane D — Landing page (abendrot.app on Vercel)** (depends on brand from C + product screenshots from B): cinematic hero, cool↔warm demo, "tiny native" glass badges, the "audit the engine" code snippet, science/trust block, OG/social cards, SEO (§10 + §21.5). Vercel tooling is available for deploy + preview URLs; matthewball.me (WordPress) only hosts the 301-redirect.

**Cross-cutting (all lanes):** run **verification/QA in a SEPARATE lane — never self-approve** (use `verifier` / `code-reviewer`); honor every **§21 audit refinement**; coordinate via a shared task list; commit per the OMC protocol. The full QA matrix — failure-injection tests (§21.1‑E14) + the self-hosted hardware matrix (M5 Tahoe gamma-broken, M3/M4, Apple display, DDC monitor, HDMI/dock) — is §8; the **1.0 launch is gated on it**.

**Decisions still mine — ASK me before doing any of these:** creating the public `matthewrball/abendrot` GitHub repo (and whether to push the internal planning/research docs publicly or keep a private build repo first); pushing commits to any remote; deploying the landing page to a live domain; or posting anything externally (Product Hunt, Reddit, social, awesome-list PRs). Do none of these without my explicit go-ahead.
