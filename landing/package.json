{
  "name": "abendrot-landing",
  "private": true,
  "version": "0.0.0-preview",
  "type": "module",
  "description": "Abendrot landing page (Lane D) — standalone static site for abendrot.app. PREVIEW only; do not deploy.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4317"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
